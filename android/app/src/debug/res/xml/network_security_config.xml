<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-variant only. Lets a debug build reach a local dev server over
  plain HTTP via `adb reverse tcp:8000 tcp:8000`, without weakening the
  release build, which has no network security config and gets the
  platform default (cleartext blocked).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
