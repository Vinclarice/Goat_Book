<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The only permission this app needs. Capture talks to one host over
         HTTPS and does nothing else; anything beyond this is a signal that
         the app has grown past what Bittern scoped it to do. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Clarice Capture"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClariceCapture">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
