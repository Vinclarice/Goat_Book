<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the UI; this exists so the window has a theme before
         the first frame and the app does not flash the platform default. -->
    <style name="Theme.ClariceCapture" parent="android:Theme.Material.NoActionBar" />
</resources>
