# The production droplet, and the only one. This file was called
# staging-inventory.ini and named the host staging.vinclarice.com, but that
# staging environment no longer exists and had always been this same droplet
# -- so the old name invited someone to treat production as a scratch target.
# The playbook's defaults (site_domain: vinclarice.com,
# deployment_environment: production) are the correct ones for this host.
[production]
vinclarice.com ansible_host=157.230.93.244 ansible_user=elspeth
