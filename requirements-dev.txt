# Extends requirements.txt with packages only needed to run the test
# suite (lists/tests/test_views.py parses rendered HTML with lxml's
# cssselect support) -- not needed in the production image, so they're
# kept out of requirements.txt to avoid bloating it.
-r requirements.txt
lxml==6.1.1
cssselect==1.5.0
# Drives the browser smoke suite (src/functional_tests). Needs its browser
# binary fetched separately -- `python -m playwright install chromium` --
# which is why those tests are their own test label and their own CI job
# rather than part of the ordinary run.
playwright==1.62.0
